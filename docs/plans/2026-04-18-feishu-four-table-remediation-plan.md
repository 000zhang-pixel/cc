# Feishu 四表整改与落地执行计划

> 目标：先确认表结构整改，再通过代码/SDK/Windows Claude Code 执行记录级修改与新增，最终落地到飞书多维表。

## 当前事实

- 本仓库已具备飞书多维表 SDK 封装：`middleware/adapters/feishu.py`
- 本仓库已具备建表/建字段脚本：`scripts/setup_base.py`、`scripts/lib/lark_client.py`
- 本机当前 **没有** 独立 Feishu CLI 可执行文件
- 本机当前 shell 环境 **没有** `LARK_APP_ID` / `LARK_APP_SECRET` / `LARK_BASE_TOKEN`
- `middleware/config/system.yaml` 中已配置四张目标表 table_id：
  - strategy: `tblu2EzR78tWjYf6`
  - shotplan: `tbl0xCaqru1TjwzK`
  - scene: `tbliWlwiyA4sppgY`
  - persona: `tblxSbOoZ2kMCkc6`

## 两阶段实施方案

### Phase 1：确认结构整改方案（当前先做）

输出物：
1. 字段/枚举整改清单
2. 记录级修改、新增、归档执行表
3. Windows Claude Code 可执行说明

### Phase 2：实际落地到飞书表

执行顺序：
1. 修改仓库中的表定义与种子脚本
2. 用 Windows Claude Code 或可用环境执行脚本
3. 用 Feishu SDK / CLI 对飞书多维表执行字段变更、记录更新、记录新增
4. 导出飞书最新数据重新核验

---

## 一、表结构整改方案（确认后实施）

### 1. Strategy 表

#### 必保留字段
- 策略编号
- 策略名称
- 适用内容类型
- 适用平台
- 适用品类
- 是否启用
- 优先级
- 系统提示词前缀
- 文案叙事节点
- 标题写作指南
- 情绪基调

#### 必补强字段
- 标题句式池
- 叙事角度标签
- 结构模式
- 差异化提示模板
- 人设适配标签
- 正文禁忌

#### 结构动作
- 将 `文案叙事节点` 从“文本/箭头串”标准化为 JSON 数组
- `适用品类` 枚举统一围绕：`手机壳 / 手机链 / 充电宝`

### 2. ShotPlan 表

#### 必保留字段
- 方案编号
- 方案名称
- 适用内容形态
- 适用内容类型
- 适用品类
- 角色序列
- 是否启用

#### 必新增/必启用字段
- 禁止重复镜头
- 动作变化要求

#### 结构动作
- `适用内容形态` 统一为运行时兼容枚举：推荐值 `图片`
- 如保留旧值，则代码必须兼容：`图片` / `图片生成` / `组图`
- 清理旧内容类型枚举：如 `种草图文`、`产品展示`

### 3. Scene 表

#### 必保留字段
- 场景编号
- 场景名称
- 是否启用
- 权重
- 适用品类
- 场景描述_中文
- 风格基调词
- 排除描述
- 人物类型
- 性别倾向
- 年龄段
- 外貌风格
- 姿态倾向
- 光线方向
- 色温
- 景深感
- 镜头感

#### 必新增/必补强字段
- 场景主题
- 道具建议
- 适合人设标签

#### 结构动作
- 统一 `人物类型` 枚举，推荐：
  - 有人物·主体
  - 有人物·局部
  - 无人物·纯产品
  - 可有人物
- `适用品类` 统一为：`手机壳 / 手机链 / 充电宝`
- 将 `充电器` 清理为 `充电宝` 或下线

### 4. Persona 表

#### 必保留字段
- 人设编号
- 人设名称
- 是否启用
- 优先级
- 适用品类
- 适用内容类型
- 性别倾向
- 年龄段
- 外貌风格
- 穿搭风格
- 动作倾向
- 气质标签
- 适合场景标签
- Prompt描述模板
- 一致性锚点模板

#### 结构动作
- 保持稳定，不做大改
- 只做标签体系对齐：与 Scene / Strategy 对齐

---

## 二、记录级评估与执行建议

## Strategy 表（30 启用）

### 已发现问题
- 30/30 条记录缺少全部高级增强字段
- 20/30 条记录的 `文案叙事节点` 仍为纯文本 fallback，未结构化

### 执行动作
- 修改：30 条
- 新增记录：建议新增 4 条高价值策略
- 下线：暂不建议立即下线

### 建议新增 4 条策略
1. 手机链穿搭点睛型
2. 手机壳细节质感型
3. 手机壳+手机链组合搭配型
4. 礼物送女生场景型

### 具体修改内容
- 每条补：`标题句式池` / `叙事角度标签` / `结构模式` / `差异化提示模板` / `人设适配标签`
- 20 条将 `文案叙事节点` 改为 JSON 数组

## ShotPlan 表（39 启用）

### 已发现问题
- 39/39 条 `适用内容形态` 与运行时不匹配
- 39/39 条缺 `禁止重复镜头`
- 39/39 条缺 `动作变化要求`
- 21/39 条内容类型使用异常枚举（`种草图文` 或 `产品展示`）

### 执行动作
- 修改：39 条
- 新增记录：建议新增 4 条
- 下线/归档：对内容类型无法映射的记录再二次审批

### 建议新增 4 条 ShotPlan
1. 手机壳装机效果 4 图
2. 手机壳+手机链组合搭配 5 图
3. 手机链细节质感测评 5 图
4. 礼物开箱展示 5 图

### 异常记录组
- `SP009`：含旧枚举 `产品展示`
- `SP020` ~ `SP039`：含旧枚举 `种草图文`

## Scene 表（91 启用）

### 已发现问题
- 91/91 条缺 `场景主题`
- 91/91 条缺 `道具建议`
- 91/91 条缺 `适合人设标签`
- 64/91 条 `人物类型` 使用非标准枚举
- 13 条缺关键技术参数（姿态/光线/色温/景深/镜头）
- 6 条含异常品类 `充电器`

### 执行动作
- 修改：91 条
- 新增记录：建议新增 6 条核心场景
- 下线/归档：暂不建议先删，先统一枚举和补值

### 建议新增 6 条 Scene
1. 手机壳+手机链组合桌面平铺
2. 女性送礼礼盒场景
3. 职场通勤电梯/办公室镜面场景
4. 书店/咖啡馆轻文艺手机链场景
5. 极简冷调棚拍手机壳场景
6. 无人物高质感桌面氛围场景

### 重点修复记录组
- `SC029` ~ `SC091`：人物类型与技术参数问题最集中
- `SC011` / `SC014` / `SC019` / `SC025` / `SC027` / `SC028`：品类含 `充电器`

## Persona 表（12 启用）

### 已发现问题
- 关键运行时字段完整，当前无结构性缺口

### 执行动作
- 修改：建议 12 条仅做标签对齐校验
- 新增记录：建议新增 2~4 条，可后置
- 下线：无

### 可选新增 4 条 Persona
1. 潮流街头手机链女生
2. 送礼向精致女生
3. 极简质感白领女性
4. 中性简洁穿搭人设

---

## 三、飞书落地执行表（第一版）

| 表 | 当前启用 | 建议修改 | 建议新增 | 建议下线/归档 | 关键动作 |
|---|---:|---:|---:|---:|---|
| Strategy | 30 | 30 | 4 | 0 | 补高级字段，结构化叙事节点 |
| ShotPlan | 39 | 39 | 4 | 0~21 待复核 | 修正形态/内容类型枚举，补镜头约束 |
| Scene | 91 | 91 | 6 | 0 | 统一人物类型，补主题/道具/人设标签 |
| Persona | 12 | 12（轻改） | 2~4 | 0 | 标签对齐，可补主营人设 |

---

## 四、代码与脚本实施点

### 需要改的仓库文件
- `scripts/tables/definitions.py`
- `middleware/handlers/content_generation.py`
- `scripts/seed_shotplans.py`
- （建议新增）`scripts/remediate_tables.py`
- （建议新增）`docs/plans/feishu-four-table-record-execution.csv`

### 推荐新增脚本能力
`remediate_tables.py` 负责：
1. 拉取四张表当前记录
2. 识别旧枚举
3. 批量更新字段值
4. 批量新增缺失记录
5. 输出修改日志和失败清单

---

## 五、执行前置条件

### 当前阻塞项
- 本机没有 Feishu CLI
- 本机 shell 未注入 `LARK_APP_ID` / `LARK_APP_SECRET` / `LARK_BASE_TOKEN`

### 可选落地路径
1. **Windows Claude Code 路径**
   - 在 Windows 环境配置 LARK 凭据
   - 执行仓库脚本或新 remediation 脚本
2. **当前仓库 SDK 路径**
   - 只要注入环境变量即可直接通过 Python 脚本写飞书
   - 不强依赖独立 CLI

---

## 六、建议的下一步

1. 先确认本文件中的结构整改方案
2. 我补一份 **逐记录执行表（CSV/Markdown）**
3. 我在仓库里补好：
   - 表定义修正
   - remediation 脚本
   - 新增种子数据模板
4. 你把这套交给 Windows Claude Code 执行到飞书
5. 执行后再导出飞书表做二次核验
