# BATCH-20260424-05 关键词清理 dry-run / apply 判定结果单

- 批次：BATCH-20260424-05
- 日期：2026-04-24
- 范围：基于 Base 关键词审计结果，输出字段级修正候选与 apply 判定
- 上游审计结果单：`docs/plans/2026-04-24-batch-result-BATCH-20260424-04-base-keyword-audit.md`
- 本批模式：只读复核 + 字段级候选清单
- dry-run：通过
- apply：未执行
- 涉及代码改动：无

## 1. 目标

将上一批的“表级命中统计”收敛为“字段级修正候选清单”，明确：
1. 哪些项可以直接进入首批 apply
2. 哪些项必须二次筛选，不能直接批量改
3. 若进入 apply，最小安全改动范围是什么

## 2. 本轮复核方法

基于现有只读脚本，对以下四类目标重新逐条抽样和字段级复核：

1. `tag` 表中的礼物类冲突标签
2. `content` 表中的礼物类冲突标签
3. `prompt` 表中的跨场景残留词（`校园 / 图书馆 / 旅行`）
4. `content` 表中的跨场景残留词（`校园 / 图书馆 / 旅行`）

复核关键词：
- 礼物类：`#新年礼物` / `#数码礼物`
- 场景类：`校园` / `图书馆` / `旅行`

## 3. 字段级复核结果

### 3.1 tag 表：可直接进入 apply 候选

- 表：`tag`
- table_id：`tblsnxUwqY1WgEAQ`
- record_count：124
- hit_count：2
- 命中字段：仅 `标签名称`

命中记录：

| record_id | 字段 | 当前值 | 判定 |
|---|---|---|---|
| `recvfOSrsjSGhM` | `标签名称` | `#新年礼物` | 可直接清理 |
| `recvfOSshrCzC8` | `标签名称` | `#数码礼物` | 可直接清理 |

判定依据：
- 命中范围极小，仅 2 条
- 命中字段单一，均为基础标签名本身
- 与当前手机壳 / 手机链主线不匹配
- 不涉及正文语义、场景库存或复杂上下文判断

### 3.2 content 表：礼物类标签可直接进入 apply 候选

- 表：`content`
- table_id：`tblieT8ZK8HOQt0x`
- record_count：47
- 礼物类标签命中：25
- 命中字段：仅 `标签`

字段级结论：
- 本轮复核下，礼物类命中全部落在 `标签` 字段
- 未发现需要先改正文才能处理的依赖关系
- 因此 **content 表中的礼物类标签可进入首批 apply 候选**

命中分布：
- `#新年礼物`：18 条
- `#数码礼物`：7 条

代表记录：

| record_id | 内容编号 | 标签字段 | 判定 |
|---|---|---|---|
| `recvh2ql9oqdze` | `T_260417_057_img01` | `#开箱推荐 #得物好物 #新年礼物 #潮玩好物 #得物数码` | 可直接移除 `#新年礼物` |
| `recvh2sRJPRNvr` | `T_260417_058_img01` | `#晒单 #学生党必备 #数码配件 #数码礼物 #买家秀` | 可直接移除 `#数码礼物` |
| `recvhq7NDdCBmV` | `T_260421_069_img01` | `#得物数码 #买家秀 #手机配件 #潮流数码 #新年礼物` | 可直接移除 `#新年礼物` |
| `recvhDQpwQ9iOp` | `T_260424_079_img01` | `#双11好物 #简约风 #得物开箱 #618好物 #数码礼物` | 可直接移除 `#数码礼物` |
| `recvhE4QWOomtB` | `T_260424_082_img01` | `#潮流数码 #学生党必备 #晒单 #数码礼物 #潮玩好物` | 可直接移除 `#数码礼物` |

批量范围：
- 共 25 条 content 记录
- 最小改动方式：仅改 `标签` 字段，逐条删除冲突 tag token，保留其他标签顺序与剩余值

### 3.3 prompt 表：跨场景残留词不能直接 apply

- 表：`prompt`
- table_id：`tbljMPKdYbGTb1K7`
- record_count：57
- 命中记录：5

命中字段分布：
- `子Prompt列表`：4
- `创意包JSON`：2
- `创意包摘要`：2
- `总Prompt`：2

关键样例：

| record_id | 提示词编号 | 命中字段 | 关键词 | 判定 |
|---|---|---|---|---|
| `recvhCIm50rJGA` | `P_T_260423_073_img01_I` | `子Prompt列表` | `校园 / 图书馆` | 需二次筛选 |
| `recvhDOYmzMZ1D` | `P_T_260424_077_img01_I` | `子Prompt列表` | `校园 / 图书馆` | 需二次筛选 |
| `recvhDQjH26yZ4` | `P_T_260424_079_img02_I` | `创意包JSON / 创意包摘要 / 子Prompt列表 / 总Prompt` | `图书馆` | 需二次筛选 |
| `recvhDUcjkSDqz` | `P_T_260424_080_img01_I` | `子Prompt列表` | `旅行` | 需二次筛选 |

不能直接 apply 的原因：
1. 命中字段已经进入多层生成资产（creative brief / summary / sub prompt / master prompt）
2. 同一记录往往有多字段联动，不能只删一个字段文本
3. 需要先判断这是“合法库存场景”还是“当前批次误注入”
4. 直接字符串替换会破坏 prompt 语义完整性，存在生成质量回归风险

### 3.4 content 表：正文/摘要中的跨场景词不能直接 apply

- 表：`content`
- table_id：`tblieT8ZK8HOQt0x`
- 场景类命中记录：5

命中字段分布：
- `正文`：5
- `创意包摘要`：1

关键样例：

| record_id | 内容编号 | 命中字段 | 关键词 | 判定 |
|---|---|---|---|---|
| `recvh2sXjQJmbe` | `T_260417_057_img02` | `正文` | `校园` | 需二次筛选 |
| `recvhiZzAeqzxs` | `T_260418_060_img02` | `正文` | `图书馆` | 需二次筛选 |
| `recvhw4xcnNa0C` | `T_260422_071_img01` | `正文` | `图书馆` | 需二次筛选 |
| `recvhDQqo8RuIO` | `T_260424_079_img02` | `创意包摘要 / 正文` | `图书馆` | 需二次筛选 |
| `recvhE4QWOomtB` | `T_260424_082_img01` | `正文` | `校园 / 图书馆` | 需二次筛选 |

不能直接 apply 的原因：
1. 正文中的场景词属于自然语言内容，不是独立 tag token
2. 直接删词会破坏文案可读性
3. 部分记录同时带有“通勤/书桌/学生党”等混合语义，需要按单条内容重写而不是字符串替换
4. 若该记录已产出配图或作为样本证据引用，直接改正文会影响追溯一致性

## 4. apply 判定

### 4.1 可安全进入首批 apply 的范围

**批次 A：礼物类冲突标签清理**

可直接进入 apply 的记录范围：
1. `tag` 表 2 条基础标签词
2. `content` 表 25 条 `标签` 字段中的 `#新年礼物 / #数码礼物`

建议 apply 方式：
- `tag` 表：
  - 若治理策略允许删除源标签，则下线或替换这 2 条基础标签记录
  - 若暂不删源标签，则至少标记 deprecated / 停用，不再参与回填
- `content` 表：
  - 仅删除 `标签` 字段中的冲突 token
  - 不改标题、不改正文、不改图片、不改指纹字段

### 4.2 不应直接进入首批 apply 的范围

**批次 B：跨场景残留词治理**

以下暂不建议直接 apply：
1. `prompt` 表命中的 5 条记录
2. `content` 表正文/摘要命中的 5 条记录
3. `strategy / scene / persona / shotplan` 中命中的库存类记录

建议前置动作：
- 先形成“误注入候选清单”
- 再由内容侧逐条判定：
  - 是否与当前产品/批次不匹配
  - 是否需整体重写而非删词
  - 是否影响样本证据链

## 5. dry-run 结果

dry-run 结论：**通过**

本轮已经收敛出明确候选范围：
- 可直接 apply：`tag(2) + content标签(25)`
- 暂不直接 apply：`prompt场景(5) + content正文/摘要场景(5) + 关联表库存项`

## 6. 风险

### Critical
1. 若只清 content 而不处理 tag 源词，冲突标签可能再次回流
2. 若直接对 prompt/content 正文做字符串删除，容易造成语义残缺或样本证据失真

### Important
3. 部分 content 记录同时承担样本证据作用，apply 前需确认是否要保留审计前快照
4. prompt 命中记录涉及多字段联动，后续应按“重建 creative brief / prompt”处理，而不是逐字段打补丁

## 7. 推荐下一动作

### 下一批建议

1. **批次 A（可执行）**
   - 先出 `tag + content标签` 的字段级 apply 清单
   - 执行前保留审计快照
   - apply 后复扫确认 `#新年礼物 / #数码礼物` 是否清零

2. **批次 B（需内容协作）**
   - 对 `prompt/content` 的 `校园 / 图书馆 / 旅行` 形成误注入候选包
   - 交由 `大C_内容总监` 做结构/结果判定后再决定重写或重跑

## 8. 输出物

- 本结果单：`docs/plans/2026-04-24-batch-result-BATCH-20260424-05-keyword-cleanup-dryrun-decision.md`
- 上游审计结果单：`docs/plans/2026-04-24-batch-result-BATCH-20260424-04-base-keyword-audit.md`

## 9. 当前结论

当前已经具备首批 apply 的最小安全范围：
- **可以先动**：`tag` 表 2 条源标签 + `content` 表 25 条标签字段冲突 token
- **不能直接动**：`prompt/content` 中的 `校园 / 图书馆 / 旅行` 文本内容，需要按记录级重写/重跑判定
